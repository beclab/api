---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: appaccessgrants.app.bytetrade.io
spec:
  group: app.bytetrade.io
  names:
    categories:
    - all
    kind: AppAccessGrant
    listKind: AppAccessGrantList
    plural: appaccessgrants
    shortNames:
    - aag
    singular: appaccessgrant
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.appName
      name: application name
      type: string
    - jsonPath: .spec.user
      name: user
      type: string
    - jsonPath: .spec.grantedBy
      name: grantedBy
      type: string
    - jsonPath: .status.state
      name: state
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AppAccessGrant grants a non-admin user permission to open a running
          application that lives in their own user-space. Owner/admin users are
          implicitly granted by their role and do not need this CR.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          spec:
            properties:
              appName:
                type: string
              appNamespace:
                type: string
              grantedBy:
                type: string
              user:
                type: string
            required:
            - appName
            - appNamespace
            - grantedBy
            - user
            type: object
          status:
            properties:
              grantedAt:
                format: date-time
                type: string
              state:
                description: pending, active
                type: string
            required:
            - state
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
